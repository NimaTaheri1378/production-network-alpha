# Synthetic demo dataset

This directory contains small synthetic data for public smoke tests only. It is generated independently and contains no WRDS, CRSP, RavenPack, Compustat, IBES, TAQ, or other vendor records.

Run:

```bash
python examples/synthetic_demo/run_synthetic_demo.py
```
